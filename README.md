# ScRNA-sequencing-Malika-

The code files will need to be downloaded to run. 
Please download the packages specified in each code file to run the file. The command to download packages in R is install.packages() in the RStudio console. Note that the Bioconductor package should be downloaded first using install.packages("BiocManager"). 
